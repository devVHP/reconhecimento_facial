# 🧠 Sistema de Reconhecimento Facial com Controle de Acesso

## 📋 Descrição

Este projeto implementa um **sistema de autenticação de usuários com reconhecimento facial**, desenvolvido em **Python**.  
Ele permite o **cadastro de usuários com imagem facial**, e posteriormente realiza o **login com validação biométrica** em tempo real.  

A aplicação foi desenvolvida com as bibliotecas **Streamlit**, **OpenCV**, **DeepFace**, **SQLite**, **NumPy** e **Pandas**.

---

## 🚀 Como Executar o Projeto

### 1. Instalar dependências

Certifique-se de ter o **Python 3.9+** instalado.  
Depois, execute o comando abaixo para instalar as bibliotecas necessárias:

```bash
pip install streamlit opencv-python deepface pandas numpy
```
### 2. Como rodar a aplicação

Certifique-se de entrar na pasta **"Codigos"**, onde os arquivos do projeto estão localizados.  
Em seguida, execute o comando abaixo no terminal para iniciar o sistema:

```bash
streamlit run app.py
```
